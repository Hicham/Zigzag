using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class README : MonoBehaviour {

	/* Welkom bij de LevelEditor!
     * 
     *   Voor je begint een kleine uitleg van de plugins
     *      Op de camera zit een postprocessing script en MK Glow
     *          Bij de postproccessing kan je camera effecten zoals een vignette instellen. 
     *          MK Glow regelt zoals je kan raden de glowing van de tiles.
     *          Naast de MK Glow plug kan je ook de WallMat.mat aanpassen voor extra effecten zoals kleur.
     *  
     *      How to create a level:
     *          Zet de "Tile Palette" aan bij Window -> Tile Palette
     *          Hier kan je een palette kiezen, maar sinds er maar 1 bestaat gebruiken we die.
     *          De tools in de toolbar zal je niet vaak gebruiken naast (B), (SHIFT) en soms (CRTL).
     *          Zorg ervoor dat EDIT unchecked is, anders pas je de palette aan.
     *          Selecteer de gewenste tegel en wanneer je (B) gebruikt kan je ze plaatsen waar je maar wilt.
     *          Succes! 
     * 
     */
}
